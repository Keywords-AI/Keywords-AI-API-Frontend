// In production, these are all commented out
export const envVars = {
    // FETCH_ENDPOINT: "https://api-test.keywordsai.co/",
    // AUTH_ENABLED: "true", // keep it string, so that it is also compatible with .env setups
}